namespace HyperJet;

using System;

public static partial class MathD
{
    public static D1Scalar Cos(D1Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D1Scalar.Forward(constant, da, a);
    }

    public static D2Scalar Cos(D2Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D2Scalar.Forward(constant, da, a);
    }

    public static D3Scalar Cos(D3Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D3Scalar.Forward(constant, da, a);
    }

    public static D4Scalar Cos(D4Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D4Scalar.Forward(constant, da, a);
    }

    public static D5Scalar Cos(D5Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D5Scalar.Forward(constant, da, a);
    }

    public static D6Scalar Cos(D6Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D6Scalar.Forward(constant, da, a);
    }

    public static D7Scalar Cos(D7Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D7Scalar.Forward(constant, da, a);
    }

    public static D8Scalar Cos(D8Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D8Scalar.Forward(constant, da, a);
    }

    public static D9Scalar Cos(D9Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D9Scalar.Forward(constant, da, a);
    }

    public static D10Scalar Cos(D10Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D10Scalar.Forward(constant, da, a);
    }

    public static D11Scalar Cos(D11Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D11Scalar.Forward(constant, da, a);
    }

    public static D12Scalar Cos(D12Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);

        return D12Scalar.Forward(constant, da, a);
    }

    public static DD1Scalar Cos(DD1Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD1Scalar.Forward(constant, da, dada, a);
    }

    public static DD2Scalar Cos(DD2Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD2Scalar.Forward(constant, da, dada, a);
    }

    public static DD3Scalar Cos(DD3Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD3Scalar.Forward(constant, da, dada, a);
    }

    public static DD4Scalar Cos(DD4Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD4Scalar.Forward(constant, da, dada, a);
    }

    public static DD5Scalar Cos(DD5Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD5Scalar.Forward(constant, da, dada, a);
    }

    public static DD6Scalar Cos(DD6Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD6Scalar.Forward(constant, da, dada, a);
    }

    public static DD7Scalar Cos(DD7Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD7Scalar.Forward(constant, da, dada, a);
    }

    public static DD8Scalar Cos(DD8Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD8Scalar.Forward(constant, da, dada, a);
    }

    public static DD9Scalar Cos(DD9Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD9Scalar.Forward(constant, da, dada, a);
    }

    public static DD10Scalar Cos(DD10Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD10Scalar.Forward(constant, da, dada, a);
    }

    public static DD11Scalar Cos(DD11Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD11Scalar.Forward(constant, da, dada, a);
    }

    public static DD12Scalar Cos(DD12Scalar a)
    {
        var constant = Math.Cos(a.Constant);
        var da = -Math.Sin(a.Constant);
        var dada = -Math.Cos(a.Constant);

        return DD12Scalar.Forward(constant, da, dada, a);
    }
}
